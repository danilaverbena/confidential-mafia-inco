Drop the compiled ABI here after `npm run compile` in `../contracts`:

```
cp ../contracts/artifacts/contracts/examples/ConfidentialMafia.sol/ConfidentialMafia.json ./ConfidentialMafia.json
```

(Not committed by default -- generated build output. Regenerate whenever the
contract changes.)
